<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="w-full overflow-auto h-screen p-10 bg-[#020617] text-white">
        <div class="flex flex-col gap-5">
            <h1 class="text-2xl">Edit Your Post</h1>
            <form action="/update/<%= post._id %>" class="flex flex-col gap-3" method="post">
                <textarea placeholder="What's on your mind?"
                    class="resize-none sm:w-1/3 w-full bg-transparent p-2 outline-none border-2 border-zinc-800 rounded-md"
                    name="content"><%= post.content %></textarea>
                <input type="submit" value="Edit Post"
                    class="px-3 cursor-pointer text-sm rounded-lg px-3 py-2 w-[100px] py-2 bg-gradient-to-r from-cyan-400 to-sky-600 hover:from-sky-600 hover:to-cyan-400" />
            </form>
        </div>
    </div>
</body>

</html>